name: Automated EOD Market Intelligence Pipeline

on:
  schedule:
    # 3:45 PM IST (Monday - Friday) = 10:15 AM UTC
    - cron: '15 10 * * 1-5'
  workflow_dispatch: # Allows manual trigger from GitHub Actions UI

permissions:
  contents: write

jobs:
  run-eod-pipeline:
    name: Fetch NSE EOD Bhavcopy & Update Dashboard
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: '' # Pure standard library, no pip cache needed

      - name: Execute EOD Pipeline Script
        run: |
          python eod_pipeline.py

      - name: Verify Generated JSON Payloads
        run: |
          if [ ! -f "data/market_summary.json" ]; then
            echo "::error ::data/market_summary.json was not generated!"
            exit 1
          fi
          if [ ! -f "data/nifty_view_data.json" ]; then
            echo "::error ::data/nifty_view_data.json was not generated!"
            exit 1
          fi
          if [ ! -f "data/banknifty_view_data.json" ]; then
            echo "::error ::data/banknifty_view_data.json was not generated!"
            exit 1
          fi
          if [ ! -f "data/market_events_data.json" ]; then
            echo "::error ::data/market_events_data.json was not generated!"
            exit 1
          fi
          if [ ! -f "data/sector_rotation_data.json" ]; then
            echo "::error ::data/sector_rotation_data.json was not generated!"
            exit 1
          fi
          echo "Market Summary Payload Size: $(du -h data/market_summary.json | cut -f1)"
          echo "Nifty View Payload Size: $(du -h data/nifty_view_data.json | cut -f1)"
          echo "Bank Nifty View Payload Size: $(du -h data/banknifty_view_data.json | cut -f1)"
          echo "Market Events Payload Size: $(du -h data/market_events_data.json | cut -f1)"
          echo "Sector Rotation Payload Size: $(du -h data/sector_rotation_data.json | cut -f1)"

      - name: Commit and Push Updated EOD Data
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/market_summary.json data/nifty_view_data.json data/banknifty_view_data.json data/market_events_data.json data/sector_rotation_data.json
          if git diff --staged --quiet; then
            echo "No changes detected in data payloads. Skipping commit."
          else
            git commit -m "chore(data): auto-update EOD market intelligence, nifty, bank nifty, events & sector rotation [skip ci]"
            git push
          fi
